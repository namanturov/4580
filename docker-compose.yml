services:
  rabbitmq:
    image: rabbitmq:4.2.0-management
    container_name: rabbitmq
    ports:
      - "5673:5672"    # для подключения клиентов к брокеру
      - "15672:15672"  # для доступа к админке
    environment:
      RABBITMQ_DEFAULT_USER: namanturov
      RABBITMQ_DEFAULT_PASS: 4580
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    restart: unless-stopped

volumes:
  rabbitmq_data: